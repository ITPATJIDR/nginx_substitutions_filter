# Simple nginx without JavaScript module - we'll handle transformation in Express
FROM nginx:1.25-alpine

# Copy our custom configuration
COPY default.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
