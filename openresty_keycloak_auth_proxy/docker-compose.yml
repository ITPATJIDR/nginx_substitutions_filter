version: "3.8"

services:
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    command: >-
      start-dev
      --import-realm
      --hostname-strict=false
      --hostname-url=http://192.168.101.13:8081
      --hostname-admin-url=http://192.168.101.13:8081
      --http-enabled=true
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_STRICT_HTTPS: "false"
    ports:
      - "8081:8080"
    volumes:
      - ./keycloak/example-realm.json:/opt/keycloak/data/import/example-realm.json:ro
    restart: unless-stopped

  api:
    build:
      context: ./api
    environment:
      NODE_ENV: production
      PORT: "3000"
    expose:
      - "3000"
    depends_on:
      - keycloak
    restart: unless-stopped

  oauth2-proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.6.0
    environment:
      OAUTH2_PROXY_PROVIDER: keycloak-oidc
      OAUTH2_PROXY_OIDC_ISSUER_URL: http://keycloak:8080/realms/example
      OAUTH2_PROXY_CLIENT_ID: openresty
      OAUTH2_PROXY_CLIENT_SECRET: openresty-secret
      OAUTH2_PROXY_COOKIE_SECRET: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
      OAUTH2_PROXY_EMAIL_DOMAINS: "*"
      OAUTH2_PROXY_SCOPE: "openid profile email"
      OAUTH2_PROXY_REDIRECT_URL: http://keycloak:8080/oauth2/callback
      OAUTH2_PROXY_HTTP_ADDRESS: 0.0.0.0:4180
      OAUTH2_PROXY_COOKIE_SECURE: "false"
      OAUTH2_PROXY_SET_XAUTHREQUEST: "true"
      OAUTH2_PROXY_SKIP_PROVIDER_BUTTON: "true"
      OAUTH2_PROXY_SKIP_OIDC_DISCOVERY: "false"
    expose:
      - "4180"
    depends_on:
      - keycloak
    restart: unless-stopped
    command: >
      --provider=keycloak-oidc
      --oidc-issuer-url=http://192.168.101.13:8081/realms/example
      --client-id=openresty
      --client-secret=openresty-secret
      --cookie-secret=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
      --email-domain=*
      --redirect-url=http://localhost:8082/oauth2/callback
      --http-address=0.0.0.0:4180
      --cookie-secure=false
      --set-xauthrequest=true

  openresty:
    build:
      context: ./openresty
    ports:
      - "8082:8080"
    depends_on:
      - api
      - keycloak
      - oauth2-proxy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped


